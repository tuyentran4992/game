{
  "name": "g4-neon-grid",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "build:standalone": "tsc --noEmit && vite build --mode standalone",
    "build:playgama": "tsc --noEmit && vite build --mode playgama",
    "build:ytgame": "tsc --noEmit && vite build --mode ytgame",
    "build:reddit": "tsc --noEmit && vite build --mode reddit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "preview": "vite preview"
  },
  "dependencies": {
    "@game/core": "workspace:*",
    "@game/sdk": "workspace:*",
    "phaser": "^4.2.1"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vite": "^5.4.0",
    "vitest": "^2.0.0"
  }
}