[project]
name = "game-pipeline"
version = "1.0.0"
description = "Game factory pipeline: scaffold, assets, validate, package"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]