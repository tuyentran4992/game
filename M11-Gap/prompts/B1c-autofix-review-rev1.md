Bạn là dev TypeScript. **VÒNG TỰ SỬA** cho batch B1c (đợt rev1).

Review độc lập báo FAIL đúng các mục sau (kèm bằng chứng sẵn). Sửa ĐÚNG các mục này, giữ nguyên thứ đã PASS:

[3c-arch] | A2 | FAIL | telemetry.ts:61-75 chứa `utf8Length` — codec byte chuỗi **không phải** trách nhiệm telemetry, bị save.ts:9 import để dùng ⇒ module quan sát thành thư viện text; records.ts:14+141 tự sở hữu blob riêng dù save.ts:40 đã mang ghosts/walls/dates |
[3c-arch] | A3 | FAIL | economy THẬT (BUY_RULES economy.ts:76, BADGE_RULES :119, giá/cap qua tham số :85/:100/:133). **save KHÔNG có chuỗi migration**: chỉ FIELD_RULES default-fill (save.ts:67) + cờ `fromFuture` (:109), không có registry `from/to/migrate`. **telemetry không phải ring**: `ring: Event[]` + mảng song song `lens: number[]` (134-135), drop bằng 2 lệnh `shift()` (149-150) ⇒ O(n)/lần, phải giữ 2 mảng đồng bộ tay. **i18n không có dictionary registry**: chỉ `dictEn` (i18n.ts:37), không có bảng locale→dict |
[3c-arch] | A5 | FAIL | làm thật trong `/tmp/b1cv2` (copy src, repo không đụng). (a) thêm v2+field `ribbon`: phải sửa **4 điểm** save.ts:14/:41/:77/:89 và **không có chỗ nào** để thêm "1 hàm migrate"; đo được v1 blob bị **nâng cấp im lặng** (`VER= 2`, `filled` liệt kê cả field chưa từng có). (b) badge thứ 7 ⇒ **không** chỉ sửa config: save.ts:14 `maxBadges: 6` clip (đo `BADGES_GIU_LAI= 6`), tức sửa 2 file trong đó 1 file code. (c) PASS: thêm `dictVi` không sửa `t` (đo `VI= Gợi ý \| EN= Hint`) |
[3c-arch] | A6 | FAIL | `KV.removeItem` (save.ts:29) khai báo trong cổng nhưng **0 call site** trong toàn `src/` ⇒ mọi adapter nền tảng phải implement rỗng; `keep` (save.ts:56) hàm identity; `boxOf` kiểm tra object trên input đã kiểu `Event` (telemetry.ts:96); `(ctx.inv.ink ?? 0)` phòng thủ trên field bắt buộc number (economy.ts:90) |
[3c-arch] | A8 | FAIL | trần cứng **trong logic**: save.ts:14-15 `maxAlbum:14, maxBadges:6, maxWallRow:5, maxDates:365` + records.ts:11 `wallRows:5` trùng `caps` của config/album.json (3 nguồn cho 1 số); telemetry.ts:40 `100*1024`/`snapshotKeep:32`; `STARTER_SKIN='skin_classic'` save.ts:19 trùng `starter` config/skins.json. **0 test nào đọc `config/`** (grep `config/` tests/logic chỉ ra comment "chưa tồn tại") ⇒ config hiện là vật trang trí |
[3c-arch] | A9 | FAIL | `^[0-3]+$` tồn tại **4 file**: stars.ts:13, progression.ts:63, records.ts:70, save.ts:45; codec sao đã có ở records.ts:59/73 nhưng save.ts:70 tự kiểm lại riêng ⇒ đổi thang sao = 4 file |
[3c-arch] | A12 | FAIL | 2 `catch {}` nuốt hẳn tín hiệu: save.ts:151-157 (`writeSave` vẫn trả `ok:true` dù bản gương `good` ghi thất bại — caller không bao giờ biết mirror hỏng) và telemetry.ts:164-170. 7 catch còn lại có kiểu/flag (120/130/169/205, telemetry 177/184, records 156) |
[3c-arch] | A14 | FAIL | i18n.test.ts:83-119 đọc **nguồn thô theo tên file** (`SCAN_TARGETS = economy/save/records/telemetry.ts`) + regex literal ⇒ test khoá vào filename, bỏ sót chính i18n.ts và mọi file mới;save.test.ts không pin `version`/tập field: bump v2 + field mới vẫn **22/22 xanh** ⇒ lưới an toàn có lỗ đúng chỗ migration |
[3c-arch] | A15 | FAIL | thêm "ruy băng/streak dài hạn" phải sửa **≥4 file cũ**: save.ts (4 điểm: type/SHAPE/FIELD_RULES/defaultSave), economy.ts, records.ts (nếu xếp hạng), config/album.json, i18n.ts |

LUẬT: chỉ sửa `game/src/**` + `game/tests/**` · KHÔNG đổi hợp đồng hàm công khai · KHÔNG đọc lan man (môi trường + hợp đồng đã ở system prompt) · KHÔNG commit/push · chạy `npm run gate` ĐÚNG 1 LẦN ở cuối rồi dán output thật.
THANG ƯU TIÊN khi sắp hết lượt: (1) typecheck XANH → (2) test XANH → (3) báo cáo ngắn.
BÁO CÁO: mỗi mục → file:dòng + 1 câu cách sửa · output gate · việc không làm được.
