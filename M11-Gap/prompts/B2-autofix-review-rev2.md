Bạn là dev TypeScript. **VÒNG TỰ SỬA** cho batch B2 (đợt rev2).

Review độc lập báo FAIL đúng các mục sau (kèm bằng chứng sẵn). Sửa ĐÚNG các mục này, giữ nguyên thứ đã PASS:

[3c-arch] | A1 | FAIL | Chiều đúng: `grep -nE "^import" src/logic/*.ts` ⇒ 0 import ngoài logic; `grep -rnE "phaser|render|require\(" src/platform/*.ts src/main.ts` ⇒ 0. **Rò**: `src/main.ts:35` `typeof localStorage === 'undefined' ? null : localStorage` — tên nền tảng nằm ngoài `src/platform`, và tạo ĐƯỜNG THỨ HAI ghi KV (bootRecords/persistRecords main.ts:39-48) bỏ qua `adapter.storage`, nên `?level` ephemeral (`debug.ts:103`) không chặn được hồ sơ |
[3c-arch] | A3 | FAIL | Null Object THẬT ở phần lớn: `sdkAdapter.ts:106,117,137,142,177,195` rơi về `offline` từng lời gọi; `grep -rnE "if *\(.*ads|if *\(.*available|platform *===|instanceof"` trên `src/` ⇒ 0 vi phạm phía caller (chỉ có gate shape trong adapter). Nhưng `available()` nói láo: `playgamaAdapter.ts:84`, `ytgameAdapter.ts:83`, `adMock.ts:65` hardcode `available: true` ⇒ kênh "host có nhưng hết ad" không biểu đạt được, caller sẽ phải suy ra ngoài interface |
[3c-arch] | A6 | FAIL | Abstraction thừa/trùng: `adMock.ts:14` phát minh `AdOutcome='completed'|'unavailable'` ĐỐI NGẪU với `types.ts:19` rồi cần bảng dịch `BY_MOCK_OUTCOME` (adMock.ts:46) — mock trả thẳng hợp đồng là xoá được 1 lớp. `PlatformAds.available()`: 0 lời gọi production ngoài platform (`grep -rn "available()" src/` lọc ra chỉ định nghĩa) |
[3c-arch] | A8 | FAIL | Ngưỡng nghiệp vụ rò xuống platform và bị nhân bản: `debug.ts:34-35` `MIN_LEVEL=1 / MAX_LEVEL=120` trùng `logic/progression.ts:37 CAMPAIGN_LEVELS = 120` — đổi chiến dịch thành 150 màn thì `?level=150` âm thầm bị `levelFrom` (debug.ts:72) trả null |
[3c-arch] | A9 | FAIL | `tests/platform/helpers/fakes.ts` **0 dòng import** (`grep -nE "^import" fakes.ts` → rỗng) nhưng khai lại `RewardedPlacement`(75) `AdPlacement`(77) `AdOutcome`(79) `AdCallResult`(80) `PlaygamaAdEvent`(190) `YtgameAdEvent`(260) `PLATFORM_STORAGE_KEYS`(83) — hợp đồng tồn tại 2 nơi, đổi `types.ts` không làm đỏ test. Cộng trùng 120 (A8) và 2 đường ghi KV (A1). Dòng/file: logic 3690, platform 908, main 48 |
[3c-arch] | A12 | FAIL | 6 `catch`, trong đó **2 chỗ nuốt tuyệt đối không tín hiệu**: `nullAdapter.ts:61` và `sdkAdapter.ts:122` (chỉ chú thích "nợ B3 nối telemetry"; `grep -rn telemetry src/platform/` ⇒ đúng 1 dòng comment, 0 call). sdkAdapter.ts:75 và debug.ts:44 thì OK vì quy về giá trị quan sát được ('failed'/token gốc) |
[3c-arch] | A14 | FAIL | Test tự khai oracle: `registry.test.ts:26-31` so bề mặt với `ADAPTER_METHODS` do `fakes.ts:112` tự liệt kê (không nhập từ `src/platform/types.ts`) ⇒ adapter thừa/thiếu method vẫn xanh nếu quên cập nhật fakes; `registry.test.ts:25` khoá cứng danh sách 3 tên ⇒ test là rào cản mở rộng (ngược A15) |

LUẬT: chỉ sửa `game/src/**` + `game/tests/**` · KHÔNG đổi hợp đồng hàm công khai · KHÔNG đọc lan man (môi trường + hợp đồng đã ở system prompt) · KHÔNG commit/push · chạy `npm run gate` ĐÚNG 1 LẦN ở cuối rồi dán output thật.
THANG ƯU TIÊN khi sắp hết lượt: (1) typecheck XANH → (2) test XANH → (3) báo cáo ngắn.
BÁO CÁO: mỗi mục → file:dòng + 1 câu cách sửa · output gate · việc không làm được.
