# Paper Crease — luật dự án (CLAUDE.md)
MARKER-CLAUDE-MD-7f3
Môi trường đã dựng sẵn: vitest config có rồi, test ở `tests/logic/*.test.ts`, import tương đối `../../src/logic/...`.
Cổng máy: `npm run typecheck && npm run test:logic`. CẤM: Math.random, đổi hợp đồng hàm, commit/push.
