# NIGHT — batch B1c
- Thời gian: 140.6 phút
- Cổng cuối: PASS
- Review FAIL còn lại: 10

## Mục review còn FAIL (cần Hermes xử)

- [3b-stress] | C6 | FAIL | PC-16: `save.ts:43-52` ghi **không trần byte** — blob 6.149.169 B vẫn `ok:true, mirror:'written'` (đo). PC-19: `i18n.ts:66-75` để lọt `{n}` trần kèm `missing:false` (xem F5) |
- [3b-stress] | C9 | FAIL | biên input: `applyWrite({ram: undefined, storage: undefined})` **NÉM TypeError** (`save.ts:58` chỉ chặn `=== null`) trong khi `LoadResult.save?: undefined` (`save.ts:70`) là nhánh hợp lệ sau `kv_unavailable`. Repro: `loadSave(KV ném semua) ⇒ {ok:false,reason:'kv_unavailable',save:undefined}` → `applyWrite({ram: failed.save, storage: failed.save})` → `THROW TypeError: Cannot read properties of undefined (reading 'rev')` |
- [3b-stress] | F3 | FAIL | v0 thiếu 6 field → điền đủ (`{version:1,level:7,stars,ink:42,skins_owned:[skin_classic],album_items:[],badges:[],sound_on:false,rev:0,ghosts:[],walls:[],dates:[]}`), `skins_owned/album_items/badges` **NGUYÊN** (2 skin, 1 album, 1 badge, ink 77, rev 5), migrate **idempotent** (`1==2? true, 2==3? true`). NHƯNG `ghosts/walls` dạng bản ghi (`[{levelIndex,ms,wrongTaps}]`) bị **xóa sạch** vì 0→1 không chuyển đổi: `filled:["ghosts"]` có tồn tại trong `migrateSave` nhưng `ParseResult` chỉ có 2 khoá `ok,save` ⇒ `loadSave` trả `{ok:true, source:'main', repaired:false, ghosts:[]}` **không một tín hiệu**, rồi `writeSave` (rev+1) ghi bản trống xuống ⇒ **mất vĩnh viễn**. Repro tối thiểu: `main = serialize({...defaultSave, ghosts:['1:900:0','2:700:1','3:650:0', null]})` → `loadSave` → `ghosts=[]` (cả 3 dòng hợp lệ chết vì 1 phần tử rác), `walls` giữ nguyên → sau 1 lần `writeSave`, KV còn `ghosts.length=0`. Cùng kiểu: `walls:[[1,2,3],['x']] → []` |
- [3b-stress] | F5 | FAIL | 500 key random + `''`,`' '`,`'.'`,`'{'`,`'{}'`,`'__proto__'`,`'constructor'`,`'toString'`,`'__defineGetter__'` ⇒ **0 ném, 0 trả `undefined`**, key lạ trả chính key. VI PHẠM: `lookup('hud.continue', dictEn)` (không truyền `n`) → `{text:'Continue — Level {n}', missing:false}` (`i18n.ts:66-69` giữ nguyên placeholder, `i18n.ts:75` gắn `missing=false`) ⇒ `{n}` trần lên UI mà **không khai báo missing**; `t()` lại vứt cờ missing. Ngoài ra `t(key, null)` / `t(key, undefined)` **NÉM TypeError** (`i18n.ts:73` `dict[key]`) trái dòng "không bao giờ ném" ở `i18n.ts:78` |
- [3b-stress] | F8 | FAIL (test tự làm oracle) | `save.test.ts:183` `expect(r.save).toEqual(defaultSave())` + `:184` `expect(serializeSave(r.save)).toBe(text)` (so 2 output của src với nhau), `:254` `:264` `toBe(defaultSave().sound_on)`, `:353` `toEqual(defaultSave().skins_owned)`, `:431` `toBe(defaultSave().level)`, `:227` `migrated` so với chuỗi dựng từ `MIGRATIONS` ⇒ default đổi theo là test vẫn xanh. Đối chiếu: `telemetry.test.ts:20,47` và `records.test.ts:31,73` dùng TextEncoder/hằng viết tay → **không** self-oracle |
- [3c-arch] | A7 | FAIL | Hệ số nằm TRONG code thay vì inject: `CAPS` (cache.ts:25-37) chép tay 5 số của config/album.json; saveSchema.ts:70,78,79,82,86,88 đọc `CAPS` trực tiếp trong khi economy.ts:103,133 lại nhận `cap` qua tham số — cùng 1 con số, 2 kỷ luật; drift chỉ do tests/logic/config-caps.test.ts:21-25 chặn, không do cấu trúc |
- [3c-arch] | A9 | FAIL | Bộ kiểm thô bị chép 3 lần: saveSchema.ts:36-40 (isInt/isStr/isPlain) vs records.ts:40-44 (isInt/isNum/isStr/isPlain) vs telemetry.ts:46-54 (isNum/isStr/isBox) — 3 bản copy cùng khái niệm |
- [3c-arch] | A10 | FAIL | `events()` trả CHÍNH object bên trong ring (telemetry.ts:213) và `normalize()` copy `data` nông (dòng 109) ⇒ caller sửa nested làm sai `len` đã cache ⇒ `bytesOf()` (dòng 131) lệch JSON thật. Ngoài ra các hàm economy/records/save đều không mutation input (buySkin:89-93, copyBadges:127, copyRows:60, migrateSave:48 `{...raw}`) |
- [3c-arch] | A12 | FAIL (nhẹ) | 0 `catch {}` rỗng, kv_error/storageDown đều báo ra ngoài (save.ts:30-37,48; telemetry.ts:171). Lỗ hổng: `restore()` bắt lỗi JSON.parse rồi `return` im lặng (telemetry.ts:187-190) — snapshot đọc hỏng mà không có cờ nào; và `applyWrite` rev RAM/storage bằng nhau ⇒ im lặng chọn 'write-storage' (save.ts:62) |
- [3c-arch] | A14 | FAIL | save.test.ts:227 `expect(migrateSave(V0_RAW).migrated).toEqual([steps[0].from+'->'+steps[0].to])` chốt cứng registry 1 dòng ⇒ thêm version 2 là test đỏ dù hành vi đúng (đo thật: 1/350 đỏ). Ngoài ra fixtures JSON viết tay (save.test.ts:6-7 header) + telemetry đo byte bằng TextEncoder độc lập ⇒ phần lớn test là lưới an toàn thật |
