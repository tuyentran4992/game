{
  "name": "juicy-merge",
  "version": "0.1.0",
  "description": "Juicy Merge: Fruit Pop — physics merge (Suika-style) for YouTube Playables",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node -e \"require('fs').copyFileSync('playgama-bridge-config.json', 'dist/playgama-bridge-config.json')\"",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "phaser": "^3.80.1"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vite": "^5.0.0",
    "vitest": "^1.6.0",
    "jsdom": "^24.0.0"
  },
  "packageManager": "pnpm@11.20.0"
}