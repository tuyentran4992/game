# M3 specs — lịch sử theo phiên bản

| Thư mục | Version | Trạng thái |
|---|---|---|
| `1-juicy-merge/` | v1 "Juicy Merge" (trái cây kawaii, 12 bậc, Suika-style) | ❌ **Playgama reject 27/08/2026** — "too closely replicates already published titles" (catalog có ≥4 Suika-clone + trùng tên "Juicy Merge"). Đã đóng băng. Codebase v1 vẫn là nền cơ học cho v2. |
| `2-potion-panic/` | v2 "Potion Panic" (3 nhánh nguyên tố + Heat/Brew + Stir + Orders + nồi cong) | 📝 **ACTIVE** — chờ anh Tuyền duyệt SPEC → giao agent code → QA → nộp Playgama. |

Xem `2-potion-panic/SPEC.md` §0 (vì sao không thể bị gọi là clone) và §9 (Anti-Reject Checklist trước khi Submit).
